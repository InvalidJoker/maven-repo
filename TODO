- rename to artifact repository (not maven only)
    - [x] support docker images
    - [x] support npm packages
    - support mirroring maven central f.e.
    - docker: garbage-collect blobs that no manifest references anymore
    - npm: `npm login` (legacy adduser) so tokens can be minted from the CLI
    - npm: /-/v1/search endpoint

- admin portal with setup
 - set repo name
 - set favicon

 - manage users
 - oauth
 - when adding user to repo suggest them all users and migrate to multiselect and dont trust arbitrary input

- password change
- token reroll

- add qutoa support for storage backend

- add upload limits

- whitemode